import React from "react";
import "./settings.css";

const Settings = () => {
  document.title = "Settings";

  return (
    <div className="settings">
      <h2>Settings</h2>
    </div>
  );
};

export default Settings;
